user=$1
repo=$2
tag=$3
if [ $tag == 'none' ]; then exit 0
echo tools/bin/linux/amd64/github-release upload --user $user --repo $repo --tag $tag --file -
tools/bin/linux/amd64/github-release upload --user $user --repo $repo --tag $tag --file -
